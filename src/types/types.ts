export interface User {
    email: string;
    password: string;
  }
  
  export interface MenuItem {
    title: string;
    path: string;
    icon: string;
  }
  
  export interface StatCard {
    title: string;
    value: string | number;
    icon: string;
  }
  